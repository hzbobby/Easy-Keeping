<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/add"
        android:contentDescription="@string/add_bank_type"
        android:icon="@drawable/ic_add"
        android:title="@string/add_bank_type"
        app:showAsAction="ifRoom" />
    <item
        android:id="@+id/search"
        android:contentDescription="@string/search_bank_type"
        android:icon="@drawable/menu_ic_search"
        android:title="@string/search_bank_type"
        app:showAsAction="ifRoom"
        app:actionViewClass="androidx.appcompat.widget.SearchView" />
</menu>